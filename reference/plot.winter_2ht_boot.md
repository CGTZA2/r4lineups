# Plot Bootstrap Distributions

Creates histograms showing the bootstrap distributions of parameter
estimates.

## Usage

``` r
# S3 method for class 'winter_2ht_boot'
plot(x, ...)
```

## Arguments

- x:

  A winter_2ht_boot object

- ...:

  Additional arguments (not used)
