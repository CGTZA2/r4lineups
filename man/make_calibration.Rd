% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_functions.R
\name{make_calibration}
\alias{make_calibration}
\title{Compute and Plot Calibration for Lineup Identification}
\usage{
make_calibration(
  data,
  confidence_bins = NULL,
  choosers_only = TRUE,
  lineup_size = 6,
  show_plot = TRUE,
  confidence_scale = c("auto", "0-1", "0-100"),
  ...
)
}
\arguments{
\item{data}{A dataframe with columns: target_present, identification, confidence}

\item{confidence_bins}{Numeric vector of bin edges (optional)}

\item{choosers_only}{Logical. Whether to analyze only suspect IDs (default = TRUE)}

\item{lineup_size}{Integer. Number of people in lineup (default = 6)}

\item{show_plot}{Logical. Whether to display the plot (default = TRUE)}

\item{confidence_scale}{How the confidence scale is interpreted: "auto" (default),
"0-1", or "0-100". See \code{\link{make_calibration_data}}.}

\item{...}{Additional arguments passed to make_calibration_gg()}
}
\value{
A list containing calibration data, statistics, and plot
}
\description{
Main function to compute and plot calibration analysis for eyewitness lineup data.
}
\examples{
set.seed(123)
n <- 200
lineup_data <- data.frame(
  target_present = rep(c(TRUE, FALSE), each = n / 2),
  identification = sample(c("suspect", "filler", "reject"), n,
                          replace = TRUE, prob = c(0.5, 0.25, 0.25)),
  confidence = round(runif(n, 0, 100))
)
cal_result <- make_calibration(lineup_data,
                               confidence_bins = c(0, 60, 80, 100))
print(cal_result)

}
