% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eig_functions.R
\name{make_eig}
\alias{make_eig}
\title{Main Function to Compute and Visualize EIG}
\usage{
make_eig(
  data,
  prior_guilt = 0.5,
  confidence_bins = NULL,
  show_plot = TRUE,
  plot_type = "both"
)
}
\arguments{
\item{data}{A dataframe with columns: target_present, identification, confidence}

\item{prior_guilt}{Numeric. Prior probability that suspect is guilty (default = 0.5)}

\item{confidence_bins}{Numeric vector of bin edges (optional)}

\item{show_plot}{Logical. Whether to create plots (default = TRUE)}

\item{plot_type}{Character. Which plot to create: "ig" (information gain),
"posteriors" (posterior probabilities), or "both" (default)}
}
\value{
A list of class "lineup_eig" containing:
  \itemize{
    \item eig: Expected Information Gain value
    \item response_data: Full response category data
    \item plot_ig: ggplot of information gain (if requested)
    \item plot_posteriors: ggplot of posteriors (if requested)
    \item ... (additional components from compute_eig)
  }
}
\description{
Convenience wrapper that computes EIG and creates visualizations.
}
\examples{
\dontrun{
# Basic usage
result <- make_eig(lineup_data)

# With confidence binning
result <- make_eig(lineup_data, confidence_bins = c(0, 60, 80, 100))

# Access plots
result$plot_ig
result$plot_posteriors
}

}
